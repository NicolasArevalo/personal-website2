---
import BaseLayout from '../layouts/BaseLayout.astro';

// Fetch Instagram posts from Behold.so JSON endpoints
const beholdUrlPersonal = 'https://feeds.behold.so/27sg3XMeF1aZ3vUXioer';
const beholdUrlFotos = 'https://feeds.behold.so/Q8ibmdPrMowV7U6nqnOI';

let instagramPosts = [];
let photographyPosts = [];

try {
  // Fetch personal feed
  const responsePersonal = await fetch(beholdUrlPersonal);
  if (responsePersonal.ok) {
    const dataPersonal = await responsePersonal.json();
    instagramPosts = dataPersonal.posts?.slice(0, 3) || [];
  }
  
  // Fetch photography feed
  const responseFotos = await fetch(beholdUrlFotos);
  if (responseFotos.ok) {
    const dataFotos = await responseFotos.json();
    photographyPosts = dataFotos.posts?.slice(0, 3) || [];
  }
} catch (e) {
  console.error('Error fetching Instagram feeds:', e);
}
---

<BaseLayout title="Sobre mí" description="Acerca de mí, mi historia y mis aficiones.">
  <div class="container page-content">
    <header class="page-header">
      <h1>Sobre mí</h1>
    </header>

    <div class="prose">
      <div class="intro-block">
        <p class="highlight-text">
          Hola, soy Nicolás. Soy un apasionado de la tecnología y de la vida que se
          creó un blog para compartir todo lo que tiene por decir. Actualmente tengo
          24 años pero un carácter de 40. Si quieres conocer más acerca de mi vida
          profesional, te recomiendo ir a mi <a id="to-portafolio" href="/profesional" class="gradient-link">portafolio</a>, donde comparto mis proyectos.
        </p>
      </div>

      <section class="story-section">
        <h2>¿Cómo llegué hasta aquí?</h2>
        <div class="story-text">
          <p>
            Siempre he sentido las ganas de compartir lo que me apasiona. Cuando
            encuentro una canción que me eriza la piel, o una frase que me toca el
            corazón, quiero compartirlo, y mis víctimas siempre son las mismas: mi
            mamá o mi hermana, a veces un par de primos (depende lo que sea). Me
            escuchan pero no sienten mi mensaje, y es verdad, las cosas que me gustan
            no tienen que gustarle a ellos, pero entonces siento que necesito
            compartirlo y no tengo con quien. Así que decidí encontrar una comunidad
            que de verdad quiera escucharme. ¿Podré? Lo averiguaremos.
          </p>
        </div>
      </section>

      <section class="movies-section">
        <h2>Top de películas</h2>
        <div class="movies-list">
          <a href="https://www.tiktok.com/@_niiico28/video/7150367573488504070" target="_blank" rel="noopener noreferrer" class="movie-row">
            <span class="movie-number">01</span>
            <div class="movie-info">
              <h3>El Origen (Inception)</h3>
              <p>La primera peli que me hizo 🤯</p>
            </div>
            <span class="movie-link-icon">↗</span>
          </a>
          <a href="https://www.tiktok.com/@_niiico28/video/7161172952111664389" target="_blank" rel="noopener noreferrer" class="movie-row">
            <span class="movie-number">02</span>
            <div class="movie-info">
              <h3>La Isla Siniestra</h3>
              <p>Leo es el mejor</p>
            </div>
            <span class="movie-link-icon">↗</span>
          </a>
          <a href="https://www.tiktok.com/@_niiico28/video/7168918955304750342" target="_blank" rel="noopener noreferrer" class="movie-row">
            <span class="movie-number">03</span>
            <div class="movie-info">
              <h3>Memento / Interstellar</h3>
              <p>Es genial pero se pelea con Interstellar</p>
            </div>
            <span class="movie-link-icon">↗</span>
          </a>
        </div>
      </section>

      <section class="mantras-section">
        <h2>Mantras</h2>
        <p class="section-subtitle">(Frases que recopilo para mi vida diaria)</p>
        
        <div class="marquee-container">
          <!-- Top Marquee: Scrolls Right -->
          <div class="marquee-track marquee-right">
            <div class="marquee-content">
              <span>"Solvitur ambulando"</span>
              <span class="marquee-separator">•</span>
              <span>"Silencio Bruno"</span>
              <span class="marquee-separator">•</span>
              <span>"Fortis Fortuna Adiuvat"</span>
              <span class="marquee-separator">•</span>
              <span>"Todo en lo que creas, existe"</span>
              <span class="marquee-separator">•</span>
              <span>"Solvitur ambulando"</span>
              <span class="marquee-separator">•</span>
              <span>"Silencio Bruno"</span>
              <span class="marquee-separator">•</span>
              <span>"Fortis Fortuna Adiuvat"</span>
              <span class="marquee-separator">•</span>
              <span>"Todo en lo que creas, existe"</span>
            </div>
          </div>
          
          <!-- Bottom Marquee: Scrolls Left -->
          <div class="marquee-track marquee-left">
            <div class="marquee-content">
              <span>"No es un día más, es un día menos"</span>
              <span class="marquee-separator">•</span>
              <span>"Don't let the fear destroy you"</span>
              <span class="marquee-separator">•</span>
              <span>"Si solo haces lo que ya sabes hacer, no serás más de lo que ya eres"</span>
              <span class="marquee-separator">•</span>
              <span>"No es un día más, es un día menos"</span>
              <span class="marquee-separator">•</span>
              <span>"Don't let the fear destroy you"</span>
              <span class="marquee-separator">•</span>
              <span>"Si solo haces lo que ya sabes hacer, no serás más de lo que ya eres"</span>
            </div>
          </div>
        </div>
      </section>

      {instagramPosts.length > 0 && (
        <h2>Día a día</h2>
        <p>Un vistazo rápido a mi vida personal y mis momentos espontáneos (<a href="https://www.instagram.com/_niiico28/" target="_blank" rel="noopener noreferrer" class="gradient-link" style="font-weight: 500;">@_niiico28</a>):</p>
        <div class="instagram-grid">
          {instagramPosts.map((post) => (
            <a href={post.permalink} target="_blank" rel="noopener noreferrer" class="instagram-post">
              <div class="instagram-image-wrapper">
                {post.mediaType === 'VIDEO' ? (
                  <img src={post.thumbnailUrl || (post.sizes && post.sizes.large ? post.sizes.large.mediaUrl : post.mediaUrl)} alt={post.prunedCaption || "Instagram post"} loading="lazy" />
                ) : (
                  <img src={post.sizes && post.sizes.large ? post.sizes.large.mediaUrl : post.mediaUrl} alt={post.prunedCaption || "Instagram post"} loading="lazy" />
                )}
                <div class="instagram-overlay">
                  <span class="instagram-icon">Ver en Instagram ↗</span>
                </div>
              </div>
            </a>
          ))}
        </div>
      )}

      {photographyPosts.length > 0 && (
        <h2>A través de mi lente</h2>
        <p>Mi perspectiva del mundo capturada en píxeles. Paisajes, luces y sombras bajo mi visión (<a href="https://www.instagram.com/fotosdeniiico/" target="_blank" rel="noopener noreferrer" class="gradient-link" style="font-weight: 500;">@fotosdeniiico</a>):</p>
        <div class="instagram-grid photography-grid">
          {photographyPosts.map((post) => (
            <a href={post.permalink} target="_blank" rel="noopener noreferrer" class="instagram-post">
              <div class="instagram-image-wrapper">
                {post.mediaType === 'VIDEO' ? (
                  <img src={post.thumbnailUrl || (post.sizes && post.sizes.large ? post.sizes.large.mediaUrl : post.mediaUrl)} alt={post.prunedCaption || "Photography post"} loading="lazy" />
                ) : (
                  <img src={post.sizes && post.sizes.large ? post.sizes.large.mediaUrl : post.mediaUrl} alt={post.prunedCaption || "Photography post"} loading="lazy" />
                )}
                <div class="instagram-overlay">
                  <span class="instagram-icon">Ver fotografía ↗</span>
                  {post.prunedCaption && <span class="instagram-caption">{post.prunedCaption.substring(0, 40)}{post.prunedCaption.length > 40 ? '...' : ''}</span>}
                </div>
              </div>
            </a>
          ))}
        </div>
      )}
    </div>
  </div>
</BaseLayout>

<style>
  .page-header {
    margin-bottom: 3rem;
  }

  .page-header h1 {
    font-size: 3.5rem;
    font-weight: 500;
    letter-spacing: -0.02em;
    font-family: var(--font-heading);
  }

  .prose :global(h2) {
    margin-top: 3rem;
    font-size: 2rem;
    color: var(--text-primary);
    font-family: var(--font-heading);
    font-weight: 500;
  }

  .prose :global(p) {
    font-size: 1.15rem;
    line-height: 1.8;
    margin-bottom: 1.5rem;
    color: var(--text-secondary);
  }

  .prose :global(ul) {
    margin-bottom: 1.5rem;
    padding-left: 1.5rem;
    color: var(--text-secondary);
    font-size: 1.15rem;
  }

  .prose :global(li) {
    margin-bottom: 0.5rem;
  }

  .instagram-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
    margin-top: 1.5rem;
    margin-bottom: 3rem;
  }

  .instagram-post {
    display: block;
    aspect-ratio: 1 / 1;
    overflow: hidden;
    border-radius: 8px;
    border: 1px solid var(--border-color);
    transition: border-color 0.3s ease;
    background-color: #080808;
    position: relative;
    text-decoration: none;
  }

  .instagram-post:hover {
    border-color: rgba(147, 51, 234, 0.4);
  }

  .instagram-image-wrapper {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .instagram-post img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }

  .instagram-post:hover img {
    transform: scale(1.05);
  }

  .instagram-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(0,0,0,0.8), transparent 50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-end;
    padding: 1rem;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .instagram-post:hover .instagram-overlay {
    opacity: 1;
  }

  .instagram-icon {
    color: white;
    font-size: 0.85rem;
    font-family: var(--font-body);
    font-weight: 500;
    letter-spacing: 0.05em;
    text-shadow: 0 2px 4px rgba(0,0,0,0.5);
    margin-bottom: 0.25rem;
  }

  .instagram-caption {
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.75rem;
    text-align: center;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* Creative Sections Styles */
  .intro-block {
    margin-bottom: 3rem;
  }
  .highlight-text {
    font-size: 1.5rem;
    line-height: 1.6;
    color: var(--text-primary);
    font-weight: 300;
  }
  .highlight-text p {
    font-size: 2rem;
  }
  .gradient-link {
    background: linear-gradient(to right, var(--accent-color), #da70d6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    font-weight: 600;
    text-decoration: none;
    transition: filter 0.2s;
  }
  .gradient-link:hover {
    filter: brightness(1.2) drop-shadow(0 0 8px rgba(147, 51, 234, 0.4));
  }
  
  .story-text {
    margin-bottom: 3rem;
  }
  .story-text p {
    font-size: 1.15rem;
    color: var(--text-secondary);
    line-height: 1.8;
  }

  .movies-section h2, .mantras-section h2 {
    margin-top: 4rem;
  }

  .movies-list {
    display: flex;
    flex-direction: column;
    margin-bottom: 3rem;
  }
  .movie-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: nowrap;
    padding: 1.5rem 0;
    border-bottom: 1px solid var(--border-color);
    text-decoration: none;
    transition: all 0.3s ease;
  }
  .movie-row:last-child {
    border-bottom: none;
  }
  .movie-row:hover {
    background-color: rgba(255, 255, 255, 0.02);
    padding-left: 1rem;
    padding-right: 1rem;
    border-radius: 4px;
    border-bottom-color: transparent;
  }
  .movie-number {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    font-weight: 300;
    color: var(--text-tertiary);
    margin-right: 1.5rem;
    min-width: 40px;
    transition: color 0.3s;
  }
  .movie-row:hover .movie-number {
    color: var(--accent-color);
  }
  .movie-info {
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }
  .movie-info h3 {
    margin: 0;
    font-size: 1.25rem;
    font-weight: 500;
    color: var(--text-primary);
  }
  .movie-info p {
    margin: 0 !important;
    font-size: 0.95rem !important;
    color: var(--text-secondary);
  }
  .movie-link-icon {
    margin-left: auto;
    font-size: 1.2rem;
    color: var(--text-tertiary);
    opacity: 0;
    transform: translateX(-10px);
    transition: all 0.3s ease;
  }
  .movie-row:hover .movie-link-icon {
    opacity: 1;
    transform: translateX(0);
    color: var(--accent-color);
  }

  /* Mantras Marquee Animation System */
  .section-label {
    font-family: var(--font-body);
    font-size: 0.75rem;
    color: var(--text-tertiary);
    letter-spacing: 0.1em;
    font-weight: 500;
    margin-bottom: 0.5rem;
    text-transform: uppercase;
  }

  .mantras-section h2 {
    margin-bottom: 0.25rem; /* Pull subtitle closer */
  }

  .mantras-section .section-subtitle {
    font-size: 0.85rem !important;
    margin-bottom: 2.5rem !important;
    margin-top: 0 !important;
    color: var(--text-tertiary);
  }

  .marquee-container {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    margin-bottom: 4rem;
    width: 100%;
    overflow: hidden;
    /* This creates the fade out effect on left and right edges */
    mask-image: linear-gradient(to right, transparent, black 15%, black 85%, transparent);
    -webkit-mask-image: linear-gradient(to right, transparent, black 15%, black 85%, transparent);
  }

  .marquee-track {
    display: flex;
    white-space: nowrap;
    overflow: hidden;
  }

  .marquee-content {
    display: flex;
    align-items: center;
    gap: 2rem;
    /* Use w-max to ensure content dictates size, prevent natural wrapping */
    width: max-content; 
  }

  /* Define the precise styling for the mantra text */
  .marquee-content span:not(.marquee-separator) {
    font-size: 1.1rem;
    color: var(--text-tertiary); /* Grey text */
    font-family: var(--font-body);
    font-style: italic;
    opacity: 0.8;
    transition: opacity 0.3s ease, color 0.3s ease;
  }

  /* Little dot separator */
  .marquee-separator {
    color: var(--accent-color);
    font-size: 1.2rem;
    opacity: 0.5;
  }

  .marquee-content span:hover {
    color: var(--text-secondary);
    opacity: 1;
    cursor: default;
  }

  /* The actual CSS animations */
  @keyframes scrollLeft {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-50%);
    }
  }

  @keyframes scrollRight {
    from {
      transform: translateX(-50%);
    }
    to {
      transform: translateX(0);
    }
  }

  .marquee-left .marquee-content {
    /* 30s scroll time for smooth slow movement. Infinite loop */
    animation: scrollLeft 35s linear infinite;
  }

  .marquee-right .marquee-content {
    animation: scrollRight 35s linear infinite;
  }

  /* Pause the animation nicely if the user hovers over the track */
  .marquee-track:hover .marquee-content {
    animation-play-state: paused;
  }
</style>
