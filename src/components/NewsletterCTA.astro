---
// NewsletterCTA.astro
---
<section class="newsletter-cta">
  <div class="container cta-container">
    <div class="cta-content">
      <h2>Únete a la lista de correo</h2>
      <p>Suscríbete para recibir nuevas notas y contenido exclusivo directamente en tu bandeja de entrada. Sin spam, prometido.</p>
    </div>
    
    <form class="cta-form" id="newsletter-form" action="/api/subscribe" method="POST">
      <input type="email" name="email" placeholder="tu@correo.com" required aria-label="Correo electrónico" />
      <button type="submit">Suscribirme</button>
    </form>
    
    <!-- Under Construction Alert -->
    <div id="newsletter-alert" class="newsletter-alert" style="display: none;">
      <span class="alert-icon">✨</span>
      <p>¡Pronto estará disponible! Estoy configurando el boletín.</p>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('newsletter-form');
    const alert = document.getElementById('newsletter-alert');

    if (form && alert) {
      form.addEventListener('submit', (e) => {
        e.preventDefault(); // Prevent actual submission
        
        // Ensure element is displayed before animating
        alert.style.display = 'flex';
        
        // Force a browser reflow so the transition works from display: none
        alert.offsetHeight; 
        
        // Show the alert
        alert.classList.add('show-alert');
        
        // Hide it after 4 seconds
        setTimeout(() => {
          alert.classList.remove('show-alert');
          
          // Wait for transition to finish before hiding from DOM
          setTimeout(() => {
            alert.style.display = 'none';
          }, 400); 
        }, 4000);
      });
    }
  });
</script>

<style>
  .newsletter-cta {
    background-color: var(--bg-color); 
    border-top: 1px solid var(--border-color);
    padding: 5rem 0;
    margin-top: 5rem;
  }

  .cta-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    max-width: 600px;
  }

  .cta-content h2 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
    letter-spacing: -0.02em;
  }

  .cta-content p {
    color: var(--text-secondary);
    margin-bottom: 2.5rem;
    font-size: 1.05rem;
    line-height: 1.6;
  }

  .cta-form {
    display: flex;
    width: 100%;
    gap: 0.75rem;
  }

  .cta-form input {
    flex: 1;
    padding: 0.85rem 1rem;
    border: 1px solid var(--border-color);
    background-color: transparent;
    color: var(--text-primary);
    border-radius: 6px;
    font-family: inherit;
    font-size: 1rem;
    outline: none;
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  .cta-form input:focus {
    border-color: var(--text-secondary);
    box-shadow: 0 0 10px rgba(255, 255, 255, 0.05);
  }

  .cta-form button {
    background-color: var(--text-primary);
    color: var(--bg-color);
    border: none;
    padding: 0.85rem 1.5rem;
    border-radius: 6px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    font-family: inherit;
    transition: background-color 0.2s, transform 0.1s, box-shadow 0.2s;
  }

  .cta-form button:hover {
    background-color: #fff;
    box-shadow: 0 4px 15px rgba(147, 51, 234, 0.2);
  }
  
  .cta-form button:active {
    transform: scale(0.98);
  }

  @media (max-width: 500px) {
    .cta-form {
      flex-direction: column;
    }
    
    .newsletter-alert {
      width: 90%;
      bottom: 1rem;
    }
  }

  /* Alert Toast Styles */
  .newsletter-alert {
    position: fixed;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%) translateY(100px);
    background-color: #000;
    border: 1px solid rgba(147, 51, 234, 0.4);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5), 0 0 20px rgba(147, 51, 234, 0.2);
    padding: 1rem 1.5rem;
    border-radius: 8px;
    align-items: center;
    gap: 0.75rem;
    opacity: 0;
    pointer-events: none;
    transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
    z-index: 9999;
  }

  .newsletter-alert.show-alert {
    transform: translateX(-50%) translateY(0);
    opacity: 1;
    pointer-events: auto;
  }

  .alert-icon {
    font-size: 1.25rem;
  }

  .newsletter-alert p {
    margin: 0;
    color: var(--text-primary);
    font-size: 0.95rem;
    font-weight: 500;
  }
</style>
